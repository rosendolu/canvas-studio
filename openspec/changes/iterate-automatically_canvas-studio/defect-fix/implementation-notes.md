# Implementation Notes - Defect Fix (Iterate Automatically Canvas Studio)

This scaffold documents the defect-fix activities for the Iterate-Automatically workflow:
- Fix avatar drift, mask coordinate drift, canvas resize offsets, and transformer capture ordering for /picture type rendering.
- Scope: targeted fixes with regression safeguards.
- Next steps: implement on feature branch, run PR review, and merge.
