# Defect-Fix Plan for Iterate-Automatically Canvas Studio (Phase 1-3)

Purpose:
- Stabilize core Canvas Studio interactions during daily iterate-automatically runs.
- Fix avatar drift, mask coordinate drift, canvas resize offsets, and transformer capture ordering for /picture type rendering consistency.

Scope:
- Existing defects in avatar group positioning, mask offset usage, and render element rendering path for /picture type.
- Regression safeguards and minimal risk changes.

Plan:
- A. Defect Fix (High ROI)
- B. New Feature (Moderate ROI)
- C. Iteration/Optimization (Moderate-High ROI)

Notes:
- ROI scores are indicative to guide prioritization in this session.
- This document is created to seed the Ops workflow and will be archived/merged into the OpenSpec artifacts.
