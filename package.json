{
  "name": "canvas-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source canvas editor with React + Vite + Mantine + NestJS + MongoDB",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/web\" \"npm run dev -w apps/server\"",
    "dev:web": "npm run dev -w apps/web",
    "dev:server": "npm run dev -w apps/server",
    "build": "npm run build -w packages/canvas-core && npm run build -w apps/web && npm run build -w apps/server",
    "typecheck": "tsc -p apps/web/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "concurrently": "^9.0.0",
    "postcss": "^8.4.47",
    "postcss-preset-mantine": "^1.17.0",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
