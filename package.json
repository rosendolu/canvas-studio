{
  "name": "canvas-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source canvas editor with Next.js 16 + Mantine 9 + MongoDB",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w packages/canvas-core && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/web"
  }
}
